import { LightningElement, track } from 'lwc';

export default class TodoList extends LightningElement {

    // priority levels are 1, 2, or 3
    priority;

    priority = 1;

    // Add new todo list item
    handleAddItem(even){

    }

    // Delte todo list item
    handleDeleteItem(even){

    }
}